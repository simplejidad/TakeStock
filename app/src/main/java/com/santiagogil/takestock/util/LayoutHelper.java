package com.santiagogil.takestock.util;

import android.content.res.Resources;
import android.support.annotation.ColorInt;
import android.support.v7.widget.CardView;
import android.view.View;

import com.santiagogil.takestock.R;

/**
 * Created by digitalhouse on 26/10/17.
 */

public class LayoutHelper {
/*
    public static void setBackgroundColor(View view, Integer independence){

        if(independence == 0){
            view.setBackgroundColor(Resources.getSystem().getColor(R.color.out_of_stock));
        } else if(independence < 30){
            view.setBackgroundColor(Resources.getSystem().getColor(R.color.some_independence));
        } else if(independence >=30){
            view.setBackgroundColor(Resources.getSystem().getColor(R.color.full_independence));
        }

    };

    public static int getIntFromColor(int Red, int Green, int Blue){
        Red = (Red << 16) & 0x00FF0000; //Shift red 16-bits and mask out other stuff
        Green = (Green << 8) & 0x0000FF00; //Shift Green 8-bits and mask out other stuff
        Blue = Blue & 0x000000FF; //Mask out anything not blue.

        return 0xFF000000 | Red | Green | Blue; //0xFF000000 for 100% Alpha. Bitwise OR everything together.
    }

    public static void setCardBackgroundColor(CardView cardView, Integer independence){

        if(independence == 0){
            cardView.setCardBackgroundColor(Resources.getSystem().getColor(R.color.out_of_stock));
        } else if(independence < 30){
            cardView.setCardBackgroundColor(Resources.getSystem().getColor(R.color.some_independence));
        } else if(independence >=30){
            cardView.setCardBackgroundColor(Resources.getSystem().getColor(R.color.full_independence));
        }

    };*/
}
